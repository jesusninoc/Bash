#1. Agregar a un fichero información sobre el software que está instalado en el sistema, actualizaciones y procesos que se están ejecutando.

#2. Programar una tarea cada cinco minutos, la tarea que hay que programas es (crear un log que añada el contenido):
#Crear un fichero con el contenido de todos los procesos (toda la información).

#3. Programar una tarea cada cinco minutos, la tarea que hay que programas es (crear un log que añada el contenido):
#Crear un fichero con el contenido de todos los PID de los procesos.

#4. Programar una tarea cada cinco minutos, la tarea que hay que programas es (crear un log que añada el contenido):
#Crear un fichero con el contenido del PID del proceso nano (abrir nano).

#5. Programar una tarea cada cinco minutos, la tarea que hay que programas es (crear un log que añada el contenido):
#Comprobar si el hash de un fichero coincide con el que tiene el proceso (no se ha modificado).

#6. Programar una tarea cada cinco minutos, la tarea que hay que programas es:
#Resolver mediante una tarea en el cron: sumar el valor de un fichero cada minuto.
